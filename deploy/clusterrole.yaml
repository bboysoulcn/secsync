apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: secsync-clusterrole
rules:
- apiGroups: [""]
  resources: ["secrets"]
  verbs: ["get", "list", "create", "update", "patch"]
