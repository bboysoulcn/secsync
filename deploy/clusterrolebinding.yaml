apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: secsync-clusterrolebinding
subjects:
- kind: ServiceAccount
  name: secsync-sa
  namespace: app
roleRef:
  kind: ClusterRole
  name: secsync-clusterrole
  apiGroup: rbac.authorization.k8s.io
